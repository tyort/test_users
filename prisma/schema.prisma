generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./users.db"
}

model User {
  id           Int      @id @default(autoincrement())
  fullName     String
  birthYear    Int
  profession   String
  friendsCount Int
  isVisible    Boolean
  isChecked    Boolean
}